const express = require("express");
const router = express.Router();
const notesController = require("../controller/note-controller.js");

router.get("/");
router.post("/");

module.exports = router;
